#include <stdio.h>

int main(void) {
	// your code goes here
	int t;
	scanf("%d", &t);
	while(t--){
	    int x, y;
	    scanf("%d %d", &x, &y);
	    int res = x * 10 + y * 90;
	    printf("%d\n", res);
	}
	return 0;
}

